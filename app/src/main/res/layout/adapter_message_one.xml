<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp">

    <!-- Grey bubble -->
    <TextView
        android:id="@+id/message_send"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:textColor="@android:color/white"
        android:textSize="15sp"
        android:minHeight="30dp"
        android:background="@drawable/background_rounded"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="30dp"
        android:layout_marginBottom="5dp"
        android:layout_marginStart="60dp" />

    <!-- Barclays icon -->
    <ImageView
        android:id="@+id/barclays_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/barclays_logo"
        android:layout_alignStart="@id/message_receive"
        android:layout_alignTop="@id/message_receive"
        android:layout_alignBottom="@id/message_receive"
        android:layout_marginEnd="10dp" />

    <!-- Blue bubble -->
    <TextView
        android:id="@+id/message_receive"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:minHeight="30dp"
        android:textColor="@android:color/white"
        android:textSize="15sp"
        android:background="@drawable/bg_chat_receive"
        android:layout_alignParentStart="false"
        android:layout_marginStart="30dp"
        android:layout_marginBottom="5dp"
        android:layout_marginEnd="60dp" />

</RelativeLayout>
